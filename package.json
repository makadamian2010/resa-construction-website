{
  "name": "project-abyss",
  "version": "1.0.0",
  "private": true,
  "description": "ABYSS — an immersive interactive deep ocean expedition and restoration simulator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.mjs && node --check server.js && node --check public/src/App.js && node --check public/src/sms.js && node --check public/src/main.js && node --check dist/server/index.js",
    "test": "node scripts/test-sms-link.mjs"
  }
}
